version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rayuela-frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
networks:
  rayuela_net:
    driver: bridge
    external: true
